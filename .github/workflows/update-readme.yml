name: Activity Tracker

on:
  schedule:
    - cron: "*/33 * * * *"
  workflow_dispatch:

jobs:
  auto-activity:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Random delay
        run: |
          DELAY=$((RANDOM % 1200))
          echo "Sleeping for $DELAY seconds..."
          sleep $DELAY

      - name: Maybe log a heartbeat
        run: |
          mkdir -p logs
          DO_UPDATE=$((RANDOM % 10))
          if [ $DO_UPDATE -lt 8 ]; then
            TIME=$(date -u)
            RAND=$((RANDOM % 100000))
            echo "Heartbeat at ${TIME} — ${RAND}" >> heartbeat.log
            echo "✅ Logged heartbeat: ${TIME} (${RAND})"
          else
            echo "🕒 Skipping this run (random skip)."
            exit 0
          fi

      - name: Update README with last 5 heartbeats
        run: |
          echo "Updating README with latest heartbeats..."
          echo "### 🩵 Recent Activity" > section.txt
          echo "" >> section.txt
          tail -n 5 heartbeat.log | tac | while read -r line; do
            echo "- ${line}" >> section.txt
          done
          echo "" >> section.txt
          
          if grep -q "### 🩵 Recent Activity" README.md; then
            awk '/### 🩵 Recent Activity/{flag=1;print;next}/^$/{if(flag){flag=0}else{print}}' README.md > tmp.md
            head -n -0 README.md > tmp.md
            sed -i '/### 🩵 Recent Activity/,$d' README.md
          fi
          cat section.txt >> README.md
          rm -f section.txt tmp.md

      - name: Commit and push
        run: |
          git config user.name "famouswizard"
          git config user.email "sonverser@gmail.com"
          git add README.md heartbeat.log
          git commit -m "Heartbeat update: $(date -u)" || echo "No changes"
          git push
